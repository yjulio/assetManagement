# Environment Configuration for Asset Management System
# Copy this file to .env and update with your actual values
# IMPORTANT: Never commit .env file to version control!

# Flask Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=true
SECRET_KEY=your-super-secret-key-change-this-in-production-use-random-string

# Database Configuration
# IMPORTANT: Change these credentials for production!
DB_HOST=localhost
DB_USER=user_asset
DB_PASSWORD=your-database-password-here
DB_NAME=db_asset
DB_PORT=3306

# Email Configuration (Optional - for notifications)
EMAIL_ENABLED=false
EMAIL_SENDER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_RECIPIENT=recipient@example.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Cloud Provider Specific Settings
# Uncomment and configure based on your cloud provider

# AWS RDS Example:
# DB_HOST=your-rds-endpoint.amazonaws.com
# DB_PORT=3306

# Azure MySQL Example:
# DB_HOST=your-server.mysql.database.azure.com
# DB_PORT=3306

# Google Cloud SQL Example:
# DB_HOST=/cloudsql/your-project:region:instance

# Security Notes:
# 1. Generate a strong SECRET_KEY: python3 -c "import secrets; print(secrets.token_hex(32))"
# 2. Use strong passwords for DB_PASSWORD
# 3. For Gmail EMAIL_PASSWORD, use App Passwords, not your account password
# 4. Set FLASK_DEBUG=false in production
